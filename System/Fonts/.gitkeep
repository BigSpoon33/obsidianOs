# Fonts Folder

Place custom font files here (.ttf, .otf, .woff, .woff2)

Then reference them in a CSS snippet to load them into Obsidian.

Example CSS snippet to add in `.obsidian/snippets/custom-fonts.css`:

```css
@font-face {
    font-family: 'MyCustomFont';
    src: url('app://local/path/to/vault/System/Fonts/MyFont.woff2') format('woff2');
    font-weight: normal;
    font-style: normal;
}

body {
    --font-interface: 'MyCustomFont', sans-serif;
}
```
